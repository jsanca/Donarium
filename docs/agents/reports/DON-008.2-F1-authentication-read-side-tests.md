# DON-008.2-F1 — Complete Authentication Read-Side Tests

**Status:** COMPLETE
**Owner:** Deep
**Role:** Implementation Engineer

## Summary

Completed testing coverage for DON-008.2: session verifier, authentication
middleware, and `/api/auth/me` handler. Added method check to me handler
for normalized 405 contract. Exposed `WithPrincipal` helper for testability.

## Test Coverage

### Session verifier (15 tests — `pgx/session_test.go`)

| Test | Verifies |
|---|---|
| ValidToken | Token verifies and returns correct claims |
| InvalidSignature | Token signed with different key → ErrInvalidSession |
| ModifiedPayload | One-byte payload change → ErrInvalidSession |
| ModifiedSignature | One-byte signature change → ErrInvalidSession |
| MissingSegment | Single segment → ErrInvalidSession |
| AdditionalSegments | Three segments → ErrInvalidSession |
| InvalidBase64Payload | Non-base64 payload → ErrInvalidSession |
| InvalidBase64Signature | Non-base64 signature → ErrInvalidSession |
| MalformedJSON | Valid signature, invalid JSON → ErrInvalidSession |
| EmptySubject | `sub=""` → ErrInvalidSession |
| NonUUIDSubject | Non-UUID sub → ErrInvalidSession |
| ExpiredToken | now > exp → ErrExpiredSession |
| ExpBeforeIat | exp ≤ iat → ErrExpiredSession |
| FutureIatBeyondClockSkew | iat > now+1min → ErrInvalidSession |
| NoPanicOnMalformedInput | 9 arbitrary inputs, no panic |

All time-sensitive tests use injected `fakeClock`.

### Middleware (8 tests — `http/auth_middleware_test.go`)

| Test | Verifies |
|---|---|
| MissingCookie | No cookie → 401, handler not called |
| EmptyCookie | Empty token → 401, handler not called |
| InvalidToken | Bad signature → 401, handler not called |
| ExpiredToken | Expired → 401, handler not called |
| DeletedUser | Resolver returns ErrInvalidCredentials → 401 |
| RepositoryFailure | Resolver error → 500 |
| SuccessInvokesNext | Valid → handler called with principal in context |
| SuccessInvokesNextExactlyOnce | Counter verifies single invocation |

### /api/auth/me (4 tests — `http/auth_middleware_test.go`)

| Test | Verifies |
|---|---|
| Success | 200 + correct principal JSON, no SessionToken in body |
| NoPrincipal | 401 |
| WrongMethod | 405 + Allow: GET |
| ContentType | application/json |

## Files Changed

| File | Change |
|---|---|
| `pgx/session_test.go` | Created — 15 verifier tests |
| `http/auth_middleware_test.go` | Created — 8 middleware + 4 me handler tests |
| `http/auth_middleware.go` | Added `WithPrincipal` public helper |
| `http/me_handler.go` | Added method check (GET only → 405) |

## Chi Import Resolution

Confirmed: Chi import was unused in `auth_middleware.go` and was already
removed during DON-008.2. `go vet` and `go build` pass without it.

## Test Results

```
go vet ./...    →  PASS (0 issues)
go test ./...   →  PASS (153 total)
go build        →  PASS
```

## Follow-ups

- DON-008.2R — Architecture Review by Elito
